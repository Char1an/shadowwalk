# CoolWalk uses no API keys — everything is open data (OSM, pvlib, Open-Meteo).
# This file is a placeholder so anyone forking the repo can drop secrets in
# later without touching .streamlit/config.toml.
#
# Example (not required):
#   openmeteo_api_key = "..."   # only needed for higher rate limits
